import 'package:flutter/material.dart';

class Formulapage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Formulae Page"),
      ),
    );
  }
}
